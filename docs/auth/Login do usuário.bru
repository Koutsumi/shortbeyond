meta {
  name: Login do usuário
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "fernanda@baccarini.dev",
    "password": "pwd123"
  }
}

script:post-response {
  const data = res.getBody();
  bru.setVar('token',data.data.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
