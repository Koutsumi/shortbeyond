meta {
  name: Cadastro de link
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/links
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "original_url": "https://google.com",
    "title": "Google"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
